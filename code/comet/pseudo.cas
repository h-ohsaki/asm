; 擬似命令のサンプル
PSEUDO	START		; プログラムの先頭を定義 (ラベルは必須)
L1	NOP
	RET
CONST	DC	#1234	; 定数を宣言 (ここに 0x1234 が配置される)
	DC	123, -1, #102F	; 複数のオペランドが記述できる
	DC	'Hello'	; 文字列定数も確保できる (5 ワードぶん配置される)
	DC	L1	; オペランドにラベルも指定可 (アドレスが格納される)
SPACE	DS	10	; 領域を確保する (10 ワードが確保される)
EMPTY	DS	0	; 大きさは 0 でもよい (ラベル定義のために使用)	
	END		; プログラムの末尾を明示 (ラベルは指定不可)
