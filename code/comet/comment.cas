; コメントの書き方のサンプル
COMSMPL	START	; コメントです
	LAD	GR1, 123	; これもコメントです
	LAD	GR2, 456 	; セミコロンの前に空白が必要です
	ADDA	GR1, GR2
	; コメントだけの行も OK です
	RET	; カラムは揃っていなくてもかまいせん
	DC	'文字列中の;はコメントではありません' ; ここはコメントです
	END
; コメントは行頭から始めても構いません
